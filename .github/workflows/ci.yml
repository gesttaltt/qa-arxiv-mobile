name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r automation/requirements.txt

      - name: Python formatting check (Black)
        run: black --check --diff automation/
        continue-on-error: true

      - name: Python linting (Ruff)
        run: ruff check automation/
        continue-on-error: true

      - name: Python type checking (MyPy)
        run: mypy automation/ || true

      - name: YAML linting
        run: yamllint automation/ci/ || true

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Markdown linting
        run: |
          npm install -g markdownlint-cli2
          markdownlint-cli2 "**/*.md" "!node_modules" || true

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r automation/requirements.txt

      - name: Run API and data validation tests (exclude Appium)
        run: |
          pytest automation/tests/ -m "not appium" \
            --html=test-results/report.html \
            --cov=automation \
            --cov-report=xml \
            -v
        continue-on-error: true

      - name: Upload test report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-report
          path: test-results/

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage
          path: coverage.xml
